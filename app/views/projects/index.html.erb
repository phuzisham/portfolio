<% if @projects.any? %>
  <div class="card">
    <% @projects.each do |p| %>
      <h4><%= p.title %></h4>
      <p><%= p.description %></p>
    <% end %>
  </div>
<% else %>
  <div class="card">
    <h4>No projects yet.</h4>
  </div>
<% end %>
